/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["dummyjson.com","cdn.dummyjson.com","images.unsplash.com","images.pexels.com"],
    },
};

export default nextConfig;
